# frozen_string_literal: true

# Forward declaration so individual pipeline files can open
# `module Tools::DataPipeline` without depending on _common.rb being
# loaded first (specs load some of these in isolation).
module Tools
end

module Tools
  module DataPipeline
  end
end
