plugins:
  - rubocop-rake
  - rubocop-rspec

AllCops:
  TargetRubyVersion: 3.2
  NewCops: enable
  SuggestExtensions: false
  Exclude:
    - "data/**/*"
    - "spec/fixtures/**/*"
    - "tmp/**/*"
    - "pkg/**/*"
    - "vendor/**/*"
    - "site/**/*"

# --- Layout / Style ---

Layout/LineLength:
  Max: 120
  AllowedPatterns:
    - "^\\s*#"
  Exclude:
    - "tools/**/*"  # long log lines and URL strings

Style/Documentation:
  Enabled: false

Style/StringLiterals:
  EnforcedStyle: double_quotes

Style/StringLiteralsInInterpolation:
  EnforcedStyle: double_quotes

# Weirich convention: `fail` introduces a new exception, `raise` re-throws
# inside a rescue. RuboCop's :semantic style enforces exactly that.
Style/SignalException:
  EnforcedStyle: semantic

Style/TrailingCommaInArrayLiteral:
  EnforcedStyleForMultiline: comma

Style/TrailingCommaInHashLiteral:
  EnforcedStyleForMultiline: comma

Style/FrozenStringLiteralComment:
  Enabled: true
  EnforcedStyle: always

# format("%s ...", a) is fine for positional args.
Style/FormatStringToken:
  EnforcedStyle: unannotated

# `to:` is an unavoidable keyword arg on this API. Short scope-locals (d, s, k) are fine.
Naming/MethodParameterName:
  AllowedNames: [d, s, k, v, e, a, b, x, y, n, to]
  MinNameLength: 2

Style/OneClassPerFile:
  Enabled: false

Gemspec/DevelopmentDependencies:
  EnforcedStyle: gemspec

# --- Metrics ---

Metrics/MethodLength:
  Max: 35
  Exclude:
    - "tools/**/*"

Metrics/AbcSize:
  Max: 35
  Exclude:
    - "tools/**/*"

Metrics/ClassLength:
  Max: 200
  Exclude:
    - "lib/timeprice/cli.rb"  # Thor DSL (desc/method_option per command) inflates line count

Metrics/ModuleLength:
  Max: 200

Metrics/BlockLength:
  Exclude:
    - "spec/**/*"
    - "*.gemspec"
    - "Rakefile"
    - "lib/timeprice/cli.rb"  # Thor's `no_commands do ... end` block

Metrics/CyclomaticComplexity:
  Max: 12
  Exclude:
    - "tools/**/*"

Metrics/PerceivedComplexity:
  Max: 12
  Exclude:
    - "tools/**/*"

# Keyword args are self-documenting; the Sandi-Metz-style limit is meant for
# positional args. The Provider/CountryFile metadata DSL needs ≥6 named args.
Metrics/ParameterLists:
  CountKeywordArgs: false

# --- Lint ---

Lint/ConstantDefinitionInBlock:
  Exclude:
    - "spec/**/*"
    - "lib/timeprice/cli.rb"

Lint/NonAtomicFileOperation:
  Exclude:
    - "tools/**/*"

Lint/NoReturnInBeginEndBlocks:
  Exclude:
    - "tools/**/*"

# --- RSpec ---

RSpec/ExampleLength:
  Max: 20

RSpec/MultipleExpectations:
  Max: 10

RSpec/NestedGroups:
  Max: 4

RSpec/DescribeClass:
  Enabled: false

RSpec/LeakyConstantDeclaration:
  Exclude:
    - "spec/**/*"

RSpec/InstanceVariable:
  Exclude:
    - "spec/**/*"
